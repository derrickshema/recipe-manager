# Backend API URL for server-side requests (not exposed to browser)
API_BASE_URL=http://localhost:8000

# Public API URL for client-side requests (only for public endpoints)
VITE_API_BASE_URL=http://localhost:8000
